# Backend environment variables for contact email delivery

# SMTP credentials for your email provider
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=melkamugatew11@gmail.com
SMTP_PASS=iiuj lquu wtwz gljm

# Optional: From address used by nodemailer. Defaults to SMTP_USER when empty.
SMTP_FROM=melkamugatew11@gmail.com

# The destination address where contact form submissions are sent
CONTACT_EMAIL=melkamugatew11@gmail.com

# Other backend settings
PORT=5000
NODE_ENV=development
