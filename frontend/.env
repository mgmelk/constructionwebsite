VITE_API_URL=
# Use a relative API path by default so mobile devices can proxy through the Vite dev server or same-origin backend.
# Set this to a reachable backend host only when needed, e.g. VITE_API_URL=http://192.168.0.10:5000

