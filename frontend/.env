# Use a public backend URL only in production or preview mode.
# When deploying frontend and backend separately, set this to the public backend URL.
# Example:
 VITE_API_URL=https://constructionwebsite-8h8j.onrender.com

# Replace the line below with your actual backend host when you deploy.
# VITE_API_URL=http://localhost:5000

# For local development, remove or comment out VITE_API_URL and run the backend on port 5000.

