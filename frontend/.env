VITE_API_URL=
# Leave blank so the browser uses the current frontend host and Vite proxies /api to the backend.
# For a production build, replace this with your real backend URL such as http://192.168.1.50:5000.

